import React from "react";
import { LeaderboardUI } from "@/screens/LeaderboardUI";

/**
 * This is the route file for the root URL ('/').
 * It renders the main UI component for the home page.
 */
const LeaderboardPageRoute: React.FC = () => {
  return <LeaderboardUI />;
};

export default LeaderboardPageRoute;
